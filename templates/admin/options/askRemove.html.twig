{% extends 'base.html.twig' %}

{% block body %}
<div id="add-agence" class="container container-screen">
    <div class="pop-up">
        <div class="">
            <p><span>Attention !</span><br>
            Vous êtes sur le point de supprimer l'option <span class="user">{{ option.name }}</span> pour
                {% if option.type == 'status_matrimoniale' %}
                "Statut matrimonial".
                {% elseif option.type == 'type_payment' %}
                "Types de paiment".
                {% elseif option.type == 'hair' %}
                "Cheveux".
                {% elseif option.type == 'eyes' %}
                "Yeux".
                {% elseif option.type == 'smoking' %}
                "Type de fumeur".
                {% elseif option.type == 'owner' %}
                "Type de propriétaire".
                {% elseif option.type == 'instruction' %}
                "Diplôme".
                {% elseif option.type == 'genre' %}
                "Sexe".
                {% elseif option.type == 'zodiaque' %}
                "Signe astrologique".
                {% elseif option.type == 'status_meet' %}
                "Types de rencontre".
                {% else %}
                "Préférence de contact".
                {% endif %}<br/>
                Cette action est irréversible, voulez-vous continuer ?</p>
            <div class="links">
                <a class="link-sm" href="{{ path('admin_option_remove', { id: option.id }) }}">Oui</a>
                <a class="link-sm" href="{{ path('admin_options') }}">Non</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
